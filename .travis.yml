install:
    - sudo apt-get install texlive-latex-recommended texlive-latex-extra texlive-fonts-recommended 
    - sudo apt-get install chktex
 
script:
    - make
    - chktex -W # Print version information.
    #- chktex -q -n 6 *.tex chapters.*.tex 2>/dev/null | tee lint.out
    # If lint output is non-empty report an error.
    #- test ! -s lint.
env: 
  global: 
    - "FTP_USER=pragmaswe" 
    - "FTP_PASSWORD=pr4gma20!4" 
after_success: 
    "curl --ftp-create-dirs -T uploadfilename -u $FTP_USER:$FTP_PASSWORD ftp://ftp.pragmaswe.altervista.org/build/"

